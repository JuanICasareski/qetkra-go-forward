// ANMAT — Requisitos / ensayos exigidos
// (boilerplate vacio)

import type { MedicalProductFlags } from "#rules/med/types";
import type { EvalReqs } from "#rules/med/output";

export function getRequirements(flags: MedicalProductFlags): EvalReqs {
  void flags;

  const reqs: EvalReqs = [];

  return reqs;
}
